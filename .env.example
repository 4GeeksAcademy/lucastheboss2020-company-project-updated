# Public API base URL. Do not include an endpoint path or trailing slash.
NEXT_PUBLIC_API_URL=https://api.example.com

# Do not put private API keys in NEXT_PUBLIC_* variables.
# If the backend requires one, keep it server-side and use a proxy route.
