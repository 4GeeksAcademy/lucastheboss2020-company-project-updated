[project]
name = "trackflow-supplier-management"
version = "0.1.0"
description = "TrackFlow supplier directory API"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "pydantic==2.10.4",
  "tinydb==4.8.2",
  "uvicorn[standard]==0.34.0",
]

[project.scripts]
seed = "services.suppliers.seed:seed"
